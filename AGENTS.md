# ChemTrack 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只应包含一行 `@AGENTS.md`，使 Claude Code 读到同一套规则；Codex 直接读本文件。

## 项目一句话

实验室化学品在线库存管理系统：以容器为单位、custody 保管权模型、GHS 安全合规、哈希链审计；Next.js + PostgreSQL + Prisma。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目复用前应先运行自己的 init。

## Init 配置

- Graduation provider(s): 暂无（暂缓对接 — 首次毕业时再连接知识库）
- 知识库索引: 尚未配置
- 毕业目标: 尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 若存在 `cairn/ROADMAP.md`，读它了解路线图、当前重点与开放问题（ROADMAP 可选；最小初始化的项目可能没有）。
3. 读 `cairn/LOG.md` 最新的若干条（最新条目在最上方），了解近期进展与关键决策。
4. 按当前任务需要，选读相关的 `cairn/` 知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 占位 | 写一次，之后不再动 |
| `cairn/ROADMAP.md` | 路线图与进度 | 原地更新，保持精炼 |
| `cairn/LOG.md` | 时间顺序日志 | 新条目加在顶部（最新在前），每条 ≤ 20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑写入正文小节并用 `contains` 标记；修订在 LOG 留指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针，绝不复制原文 |

> 其余文件只在出现具体信号时创建（要记录的决策、被解决的坑、跨会话的目标）——不预建空壳。工程资产（被代码或流程消费的契约/配置/规格）不归本系统管理，留在代码树中，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突以本文件为准。
- 业务/设计结论以 `cairn/` 知识专题文档中的最新记录为准，而非更早的 LOG 条目。

## 知识库消费反射

- 在开展"其可复用内核（产出或依赖的任何结论）够格毕业"的工作之前，先查阅本项目自身的 `cairn/` 知识专题文档；目前尚未连接外部知识库（provider 暂缓对接，见上方 Init 配置），外部索引检查与 `cairn/Cited.md` 引用在接入后启用。

## 文档协作规则

- 改动前先判断用户要的是"讨论/建议"还是"直接改文档"；对方说"先看看 / 先评估"时，先给分析——不要直接重写正式文档。
- 纠正过往判断时以追加更正说明的方式进行，不要静默覆盖。
- 未经确认的判断不要写成定论。

## 知识沉淀规则

- 每完成一步实质性进展，就在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；让结论沉淀到 `cairn/` 知识专题文档。
- **完成答复门禁：**在任何完成性表述之前——包括但不限于工作已完成/已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复/已解决；交付物可用；工作已结束；以及语义等价的说法——先执行 `references/maintenance.md` 中的 Cairn 检查点；只更新其触发矩阵要求的记录，核实后再答复。用户明确要求只读/不改动时，禁止任何 Cairn 写入。
- 跨项目可复用的经验，待接入知识库后经毕业机制沉淀（provider 暂缓对接，见上方 Init 配置）。
